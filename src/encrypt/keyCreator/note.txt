对于aes，des这些加密算法，密钥的长度往往有特殊的要求，这对于密钥生成方有一定的困难（比如构建一个250位的密钥）
为了解决这个问题，一般都用java系统工具类来生成密钥(种子+随机数生成算法(系统会有默认值，windows系统和linux系统不同，最好人工指定)+密钥指定算法+指定密钥长度)，在同一随机数生成算法同一密钥指定算法同一密钥长度下，种子和密钥一一对应，而且种子没有长度限制
很多时候所谓的32位aes密钥可能只是指生成32位aes密钥的种子